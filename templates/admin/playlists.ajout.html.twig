{% extends "baseadmin.html.twig" %}

{% block body %}
    <h2 class="mt-3">Ajouter une playlist</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{ path('admin.playlists') }}" class="btn btn-link mt-3">
        Retour à la liste
    </a>
{% endblock %}
